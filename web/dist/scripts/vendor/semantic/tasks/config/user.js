var extend=require("extend"),fs=require("fs"),path=require("path"),requireDotFile=require("require-dot-file"),defaults=require("./defaults"),config=require("./project/config"),gulpConfig={},userConfig;try{userConfig=requireDotFile("semantic.json")}catch(error){"MODULE_NOT_FOUND"===error.code&&console.error("No semantic.json config found")}gulpConfig=userConfig?extend(!1,{},defaults,userConfig):extend(!0,{},defaults),config.addDerivedValues(gulpConfig),module.exports=gulpConfig;